<definitions>
	{% for partOfSpeach in definitions %}
		<definition>
			<type>{{partOfSpeach.type}}</type>
			{% for d in partOfSpeach.definition %}
				<text>{{d}}</text>
			{% endfor %}
		</definition>
	{% endfor %}
</definitions>